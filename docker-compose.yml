services:
  sentiment-api:
    build: .
    container_name: twitter-sentiment-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./models:/app/models
      - ./outputs:/app/outputs
    restart: unless-stopped
